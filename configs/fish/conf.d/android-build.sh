# =============================================================================
# Android Build Environment
# =============================================================================
# When you are ready to uncomment your Android build variables for compiling
# your LineageOS tree, fish uses `set -gx` instead of `export`.
#
# set -gx USE_CCACHE 1
# set -gx CCACHE_DIR "$HOME/.cache/ccache"
# set -gx MAX_SOONG_JOBS 4
# set -gx JOBS 12
# ulimit -n 1048576
# ulimit -u unlimited

